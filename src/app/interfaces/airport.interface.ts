export interface IAirport {
  _id: string;
  name: string;
  email: string;
  phoneNumber: string;
  physicalAddress: string;
  postalAddress: string;
  postalCode: string;
  createdAt: string;
  updatedAt: string;
}
